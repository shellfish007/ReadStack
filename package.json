{
  "name": "readstack",
  "version": "0.1.0",
  "description": "A minimalist reading and notes tracker SPA",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "test": "vitest --run"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "vitest": "^1.0.0",
    "jsdom": "^24.0.0"
  }
}
